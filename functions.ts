// JS function
let logJS = function(message) {
    console.log(message);    
}

// TS Arrow functions
let logTS = (message) => {
    console.log(message);
}
// or 1 line instruction
// similar to C# and Java lambda expressions
let alternativeLogTS = (message) => console.log(message);
